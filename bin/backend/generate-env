#! /usr/bin/env ruby

require 'erb'

template = File.read '$THEIA_WORKSPACE_ROOT/erb/backend-flask.env.erb'
content = ERB.new(template).result(binding)
filename = "$THEIA_WORKSPACE_ROOT/backend-flask.env" 
File.write(filename, content)